# Local dev: OrbStack Postgres container via its OrbStack domain
# (host tools only — the app container gets DATABASE_URL from docker-compose)
# Production: Neon pooled connection string
DATABASE_URL=postgresql://hrms:hrms@apop-hrms-db.orb.local:5432/hrms
